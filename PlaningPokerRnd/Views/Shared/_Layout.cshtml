<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>Pokerapp</title>
    <link href="~/favicon.ico" rel="shortcut icon" type="image/x-icon" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    @Styles.Render("~/Content/css")
    @Styles.Render("~/Content/Materialize")
</head>
<body>
    <div class="container body-content">
        @RenderBody()
    </div>
    
    
    @*<script type="text/javascript">
    var application;
    $(document).ready(function () {
        application.config = function () {
            var appPath = '@Url.Content("~/")';
            var rootApiUrl = '@(HtmlHelpers.GetBaseUrl() + "api/")';
            var actionUrl = '@(Url.Action(string.Empty, string.Empty, null, Request.Url.Scheme))';


            return {
                appPath: appPath,
                getApiUrl: getApiUrl,
                actionUrl: actionUrl
            }

            function getApiUrl(name) {
                return rootApiUrl + name;
            }
        }
    });
    </script>*@

    @Scripts.Render("~/bundles/app")
    @Scripts.Render("~/bundles/angular")
</body>
</html>
